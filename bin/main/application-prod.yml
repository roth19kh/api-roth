spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: jdbc:postgresql://dpg-d3fn6bp5pdvs73be510g-a.oregon-postgres.render.com:5432/roth?sslmode=require
    username: roth_user
    password: ArTLhsNDeUNVYbabV93WNrQI0Og5rZUL
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false

file:
  upload-dir: /tmp/uploads

logging:
  level:
    com.setec: INFO
    org.hibernate.SQL: WARN
