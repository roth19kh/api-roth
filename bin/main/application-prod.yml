spring:
  config:
    activate:
      on-profile: production # This activates the profile
  datasource:
    url: jdbc:postgresql://roth_user:ArTLhsNDeUNVYbabV93WNrQI0Og5rZUL@dpg-d3fn6bp5pdvs73be510g-a/roth
    username: roth_user
    password: ArTLhsNDeUNVYbabV93WNrQI0Og5rZUL
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false

file:
  upload-dir: /tmp/uploads

logging:
  level:
    com.setec: INFO
    org.hibernate.SQL: WARN
